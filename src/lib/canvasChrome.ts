/** Top/bottom band height aligned with Figma Agent-Builder (spacing-s + h-10 + spacing-s). */
export const CANVAS_CHROME_TOP = 'calc(var(--spacing-s) + 2.5rem + var(--spacing-s))';
export const CANVAS_CHROME_BOTTOM = CANVAS_CHROME_TOP;

/** Floating inspector width from Figma (px). */
export const CANVAS_PANEL_WIDTH_PX = 382;
